<%- include('../partials/head', {title:'Accueil'}) %>
    <%- include('../partials/header') %>

        <main>

            <%- include('../elements/hero') %>

                <h2 class="title-2">Les dernières annonces</h2>

                <div class="announce-container">
                    <% for(let i = Math.max(0, PostedAnnounce.length - 3); i < PostedAnnounce.length; i++) { %>
                        <%- include("../elements/announce", { announce: PostedAnnounce.at(i) }) %>
                            <%} %>
                </div>

                <div class="get-announces">
                    <p class="get-announces-button">Consulter toutes les annonces</p>
                </div>
        </main>

        <%- include('../partials/footer') %>